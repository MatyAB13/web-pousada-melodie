[build]
  command = "npm run build"
  publish = ".next"

[[redirects]]
  from = "/*"
  to = "/.next/public/*"
  status = 200
  force = true

[dev]
  command = "npm run dev"
  port = 3000
  publish = "app"

# Configuración óptima para Next.js 14 App Router en Netlify
